import React from 'react';
import PropTypes from 'prop-types';
import './Header.css';

export const Header = ({}) => {
    
    return (
      <header>
        <h1>Header component</h1>
      </header>
    );
  };